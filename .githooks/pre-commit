#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

uv run pytest
uv run ruff check .
uv version --bump patch

git add pyproject.toml

if [ -f uv.lock ]; then
    git add uv.lock
fi